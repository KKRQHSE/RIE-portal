# Nachtelijke zelfstandige sessie — 6/7 september 2026

Kees is afwezig, kan niet op stop-punten reageren. Werk uitsluitend op `main`, geen branches/
worktrees. Vaste afspraken: tsc + build groen, commit + push per fase, tests met
`--use-system-ca`, geen native `confirm()`, null-veilige guards. Harde grens: geen
verwijderende/overschrijvende SQL op bestaande data, geen echte accounts/personen/records
aanraken (Dutch Waste is een echte klant). Alleen additieve, veilige wijzigingen — bij een
ontwerpkeuze of iets dat bestaande data raakt: niet doen, hier documenteren als openstaand met
een voorstel, doorgaan.

Start: 2026-09-06 21:29, main op commit `8a2f819`, werkboom schoon (alleen browser-smoke-
testscreenshots als lokale ruis, niet gecommit), database bereikbaar.

## Status
- [x] Fase 1a — onderzoek: wat kan een webapp op iOS Safari / Android Chrome echt qua "bewaar
      foto op telefoon"
- [x] Fase 1b — bouwen: bewaar-knop bij inspectie- en incidentfoto's
- [x] Fase 1c — NL/TR-tekst voor de nieuwe knop
- [x] Fase 2 — Turkse UI-dekking nalopen op /tb, /melden, en InspectieUitvoeren

---

## Fase 2 — Turkse UI-dekking (2026-09-06/07)

Alle drie werknemer-facing schermen die de bestaande NL/TR-woordenboeken gebruiken volledig
gelezen (niet alleen gegrept): `components/ToolboxGastClient.tsx` (/tb, `TB_TEKST`), `components/
IncidentMeldClient.tsx` (/melden, `MELD_TEKST`), `components/InspectieUitvoeren.tsx` (het
werknemer-invulscherm van de inspectie, `INSP_TEKST` — inclusief de nieuwe foto-bewaarknop uit
fase 1). Gecontroleerd: geen enkel bestand buiten deze drie gebruikt de woordenboeken (dus geen
vierde werknemerscherm gemist).

**Eén echte hiaat gevonden en gefixt:** `IncidentMeldClient.tsx` had een hardcoded Nederlandse
foutmelding buiten het vertaalmechanisme om (`` `Foto "${bestandsnaam}" is te groot (max 5
MB).` ``) — bleef Nederlands staan ongeacht de taalkeuze. Nieuwe sleutel `foutFotoTeGroot`
toegevoegd aan `MELD_TEKST` (zelfde tekst als de al-bestaande `INSP_TEKST`-variant), code
aangepast. `MELD_TEKST` miste ook een generieke `bezig`-sleutel (nodig voor de nieuwe
bewaar-knop uit fase 1) — toegevoegd, zelfde patroon als de andere twee woordenboeken.

**`ToolboxGastClient.tsx` (/tb) en `InspectieUitvoeren.tsx`: geen hiaten gevonden.** Elke vaste
UI-tekst loopt al door `t()`; wat NIET vertaald wordt is bewust en correct KAM-/systeeminhoud
(toolboxtekst, quizvragen/-opties/-uitleg, checklistvragen, rubrieknamen, historieregels,
gevolg-labels uit `incident_gevolg_soort`) — dat hoort zo, staat ook al zo in de code-comments,
niet aangeraakt.

**Openstaand, met voorstel (ontwerpkeuze, NIET gebouwd vannacht):** de drie "ongeldige
link"-foutschermen (`app/melden/[token]/page.tsx`, `app/tb/[token]/page.tsx`, `app/a/[token]/
page.tsx` — elk een `OngeldigeLink()`-functie) zijn SERVER-components zonder enige taal-state:
altijd hardcoded Nederlands, ook al bestaat er voor de rest van diezelfde route een NL/TR-toggle.
Dit is een randgeval (alleen zichtbaar bij een ingetrokken/verlopen link) maar wel een echt gat.
Niet vannacht gefixt omdat het een ontwerpkeuze vraagt die drie bestanden consistent raakt:
  - **Optie A (eenvoudigst, geen JS nodig):** de Nederlandse en Turkse tekst gewoon allebei
    statisch tonen (bv. NL-zin, dan de TR-zin eronder) — werkt altijd, geen client-wrapper nodig.
  - **Optie B (consistent met de rest):** een lichte client-wrapper met dezelfde `TaalWissel` +
    `useTaal()`, alleen voor dit foutscherm.
  Voorstel: optie A — een dood-lopend foutscherm hoeft geen interactieve taalkeuze, en twee talen
  onder elkaar is voor een kort zinnetje prima leesbaar. Kees: laat weten of je dit wilt en welke
  optie; dan bouw ik het in een volgende sessie.

tsc/build/lint/volledige suite (incl. de al-bestaande browsersmoke- en isolatietests) opnieuw
38/38 groen na de fix.

---

## Fase 1a — onderzoek: wat kan een webapp écht (2026-09-06, geverifieerd via actuele MDN/
caniuse-documentatie, niet uit trainingsgeheugen aangenomen)

**Een webapp mag NOOIT stil/automatisch naar de fotobibliotheek schrijven.** Dat is bewust
browser-/platformbeleid op zowel iOS als Android, geen technische beperking die te omzeilen valt
en geen bug om te fixen — er is dus geen "onzichtbare één-tik-rechtstreeks-naar-Foto's"-optie
mogelijk, op geen van beide platforms.

Twee mechanismen, in volgorde van voorkeur:

1. **Web Share API met bestanden** (`navigator.share({ files: [...] })`, met
   `navigator.canShare()` als vooraf-check). Ondersteund op zowel iOS Safari als Android Chrome.
   Vereist: secure context (https — heeft dit portaal al), en een VERSE user-gesture (moet dus
   synchroon vanuit de klik-handler zelf aangeroepen worden, niet ná een eigen `await` ervoor).
   Opent het native deelvenster; de gebruiker kiest daar zelf "Bewaar afbeelding" / "Save to
   Photos" / een andere app. Dit is op iOS de ENIGE weg die betrouwbaar in de echte
   Fotobibliotheek landt.
2. **Terugval: een directe download** (`<a download>`). Sinds iOS 13 / recente Chrome breed
   ondersteund. Op Android landt dit meestal alsnog in de Galerij, omdat Android's mediascanner
   doorgaans ook de Downloads-map indexeert. Op iOS komt een download in de Bestanden-app, NIET
   automatisch de Fotobibliotheek — een bewuste iOS-beperking.

**Conclusie/belofte die we NIET doen:** we beloven niet "met één tik altijd rechtstreeks in de
Fotobibliotheek" — dat kan een browser niet garanderen op iOS. We bouwen wél de best haalbare
versie: Share-API eerst (met het echte "opslaan in Foto's"-keuzemenu erin), automatische
terugval op download zodra Share niet beschikbaar is (bv. desktop).

Gebouwd: `lib/foto-bewaren.ts` (de kernlogica, met de bovenstaande volgorde) + `components/
BewaarKnop.tsx` (gedeelde knop, twee weergaven: compact icoon-badge op een kleine thumbnail, en
een volledige tekstknop waar meer ruimte is). Ingehaakt op:
- **Inspectie** (`components/InspectieUitvoeren.tsx`, `FotoBlok`): een klein rond knopje
  rechtsonder op elke fototegel (naast het bestaande verwijderknopje rechtsboven), gebruikt de
  al-geüploade foto's signed download-URL.
- **Incident** (`components/IncidentMeldClient.tsx`): een tekstknop naast elke net-geselecteerde
  (nog niet geüploade) foto in de lijst, gebruikt het lokale bestand rechtstreeks — geen
  netwerk-aanroep nodig, dus werkt ook vóórdat de melding is verstuurd.
De bestaande upload naar het afgeschermde portaal is in beide gevallen NIET aangeraakt — de
bewaar-actie komt er los bovenop, faalt de een niet als de ander lukt/mislukt.

NL/TR: nieuwe sleutels `fotoBewaren`/`fotoBewarenOpgeslagen`/`fotoBewarenMislukt` toegevoegd aan
zowel `INSP_TEKST` als `MELD_TEKST` in `lib/i18n-werknemer.ts` (machinevertaling, zoals de rest
van dat bestand al markeert — nog te laten nakijken door een moedertaalspreker). `MELD_TEKST`
miste een generieke `bezig`-sleutel (had alleen `versturenBezig`) — die erbij gezet, zelfde
patroon als de andere twee woordenboeken.

**Geverifieerd (headless Chromium, Playwright):** de knop verschijnt in zowel NL als TR met de
juiste tekst, en een klik triggert zonder JS-fouten de terugval-download (headless Chromium heeft
geen native deelvenster om Share tegen te testen — dat kan alleen op een echt toestel, zie de
testlijst onderaan dit rapport). tsc/build/lint/volledige testsuite: 38/38 groen.

---
