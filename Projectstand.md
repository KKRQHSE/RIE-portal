# Projectstand — QVOX RI&E

> Levend document, de enige plek voor "waar staan we". Status, geen verantwoording: het waarom van keuzes staat in `Beslissingen.md`. Overschrijf vrij bij elke sessie.
>
> **Laatst bijgewerkt:** 5 juli 2026 — **Incidenten-/ongevallen-module COMPLEET (fasen 1-4).** Nieuwe afneembare catalogusmodule 'incidenten' (migraties 0025-0027): laagdrempelig melden + KAM-afhandeling + doorklikbaar dashboard. **Fase 4 (dashboard):** bovenaan de KAM-pagina, kiesbare periode (dit jaar / 12 mnd / alles), drie niveaus — (1) aantallen per status en per gevolg + top-5 directe/basisoorzaken, (2) klik op een tegel filtert de meldingenlijst, (3) klik op een regel opent het volledige incident (Deel 1 + Deel 2 + foto's). Client-side aggregatie over de al RLS-gescopete lijst (geen extra RPC/serveroppervlak; later vervangbaar door een lees-RPC bij groot volume). `tsc` + `next build` groen. AVG-borging: gezondheidsgegevens (letsel/medische dienst/functie slachtoffer) alleen op de KAM-only-rij, nooit in de open flow; foto's privé (bucket `incident-foto`, per-bedrijf storage-RLS). Gereserveerde haken `actie_ids` + `toolbox_push_id` klaar voor latere koppeling. Beslissing 63. Daarvóór dezelfde dag: **Fase 3: KAM-afhandeling** (migratie 0027). Module 'incidenten' in de catalogus; KAM-pagina `/[company_id]/incidenten` (geguard: ingelogd + `mag_bedrijf_beheren` + module actief): lijst van meldingen → detail met Deel 1 (lezen) + foto's + Deel 2-formulier (status, directe/basis oorzaken uit de vaste lijsten, toelichting, maatregelen, gevoelige slachtoffer-/letselvelden apart gemarkeerd). Mutaties via SECURITY DEFINER-RPC's met null-veilige guard, anon dicht (`incident_deel2_opslaan`, `incident_meldlink_zorg/_roteren/_intrekken`); onbekende oorzaakcodes gefilterd; `afgehandeld_op` gestempeld. Foto's voor de KAM via `/api/incident/foto-download` (service-role signed URL ná RLS-select). Meldlink-kaart met deelbare URL + kopiëren + roteren (inline bevestiging, geen native confirm) + in-/uitschakelen, en een **QR-code via een eigen dependency-vrije encoder** (`lib/qr.ts`, geen npm-dep, geen externe QR-service → geen token-lek), geverifieerd met een round-trip-zelftest. Isolatietest 20/20, security-regressie 25/25 (61 gehardende + 13 token-RPC's), QR-zelftest groen, `tsc` + `next build` groen. Alleen fase 4 (dashboard) staat nog open. Daarvóór dezelfde dag: **Fase 2: open meldflow** (migratie 0026). Publieke, login-loze meldpagina `/melden/[token]` die via het bedrijfstoken (`incident_meldlink`) in de juiste tenant landt; mobiel-vriendelijk formulier (datum/tijd vooringevuld, locatie, project, omschrijving, gevolg aanvinken, naam optioneel, foto's) → nette bevestiging. Vier SECURITY DEFINER token-RPC's, bewust anon-toegankelijk (meldcontext/melden/foto-pad/foto-registreren); onbekende gevolg-codes server-side gefilterd; geen slachtoffer-/letseldetails in de open flow. Foto's in een NIEUWE privé-bucket `incident-foto` met échte per-bedrijf storage-RLS (correctie op het Fase 1-advies: de `bewijs`-bucket dwingt storage-zijdig alleen 'ingelogd' af, te zwak voor deze AVG-categorie); upload via service-role signed URL (`gast-upload`-patroon), geen publieke URL. Isolatietest 12/12, security-regressie 25/25 (13 token-RPC's), `tsc` + `next build` groen. Fase 3 (KAM-afhandeling) en fase 4 (dashboard) volgen. Daarvóór dezelfde dag: **Fase 1: datamodel** (migratie 0025, additief). Nieuwe afneembare catalogusmodule 'incidenten': laagdrempelig incidenten/bijna-incidenten melden + afhandelen + dashboard. Twee delen op één `incident`-rij, net als het papieren formulier: **Deel 1 (melder)** — géén login, via een vaste bedrijfseigen meldlink/QR (`incident_meldlink`-token, intrekbaar/roteerbaar, eigen token — geen hergebruik van werknemer-/actiehouder-token); minimale velden (datum/tijd/locatie/project/omschrijving/naam-melder/gevolg-aanvinken/foto's). **Deel 2 (KAM, ingelogd)** — status, directe + basis oorzaken (aanvinken uit de twee vaste genummerde lijsten: 28 directe, 16 basis, als geseede referentietabellen), toelichting, maatregelen, en de **gevoelige** slachtoffer-/letselvelden. `incident`-rij is alleen leesbaar voor KAM/admin van het eigen bedrijf (`mag_bedrijf_beheren`); muteren uitsluitend via SECURITY DEFINER-RPC's (fase 2/3), geen insert/update-policy. Foto's (`incident_foto`) in de privé `bewijs`-bucket, bedrijf-geprefixt pad, geen publieke URL. **AVG: zwaarste categorie tot nu toe** — letsel + medische-dienst + functie-slachtoffer zijn gezondheidsgegevens; bewust nooit in de open flow, alleen op de KAM-only-rij (zie AVG-punten onderaan). RLS geverifieerd (6 tabellen, RLS aan + policies), schema gedumpt, `tsc` groen. **Fase 1 is een STOP-punt** — flows (meldpagina, KAM-afhandeling, dashboard) volgen ná akkoord op het datamodel. Beslissing 63. Daarvóór (28 juni): **Echte video-afspeeldetectie in de werknemer-toolboxflow** (geen DB-wijziging). Het eerlijkheidsvinkje "ik heb de video bekeken" is vervangen door echte detectie: brontype uit `video_url` (YouTube → IFrame Player API; direct videobestand/Storage-object → HTML5 `<video>`; rest → fallback-link, uitbreidbaar voor Vimeo). "Bekeken" = afspeelpositie ≥90% van de duur (doorspoelen telt); "Volgende" hangt aan dat event. Bij een niet-afspeelbare/niet-ladende video: nette uitleg + open-in-tabblad + handmatige fallback-bevestiging (geen vastloper). Privé-Storage-video: een `storage:bucket/pad`-referentie wordt server-side (token al gevalideerd) omgezet naar een kortlevende **signed URL** — bucket blijft privé; een publieke URL gaat ongewijzigd door. **Server-contract ongewijzigd** (`vereist_video` → `p_video_bekeken` blijft vereist); isolatietest 31/31 (incl. server-gate). **Browsertest-stap (nieuw):** open een toolbox met (1) een YouTube- en (2) een bestand-bron → "Volgende" blijft uit tot ~90%, daarna actief; doorspoelen voorbij 90% activeert ook; een kapotte/onbekende bron toont de fallback i.p.v. een eindeloos uitgegrijsde knop. Daarvóór dezelfde dag: **bugfix toolbox eens-per-jaar** (migratie 0024). REGEL: één afgeronde `toolbox_deelname` per persoon × toolbox × kalenderjaar. Dubbele laag: een unieke partiële index op `(company_id, persoon_id, toolbox_id, jaar_utc(afgerond_op)) WHERE toolbox_id is not null` (zelfs een directe insert kan geen tweede record maken; ontkoppelde records blokkeren niets; nieuw jaar mag opnieuw) + een nette RPC-weigering "Deze toolbox is dit jaar al afgerond". Werknemer-UI opent een al-afgeronde toolbox niet meer in de flow (geen tweede tekenscherm). Bestaande dubbelen op Alpha opgeschoond (oudste behouden). Isolatietest 30/30, security-regressie 25/25. Daarvóór dezelfde dag: **Security-hardening: per-bedrijf-guard null-veilig aan de bron + anon-EXECUTE projectbreed ingetrokken** (migraties 0022/0023, Beslissing 62). Het tijdens de export gevonden tenant-lek (anon → `mag_bedrijf_beheren` = NULL → `if not …` sloeg de raise over) is nu drie lagen diep gedicht: bron-fix (`coalesce(...,false)`), EXECUTE ingetrokken voor anon/PUBLIC op alle 57 per-bedrijf/admin-RPC's (token-RPC's + RLS-helpers bewust open), en de norm vastgelegd voor nieuwe RPC's. Bewijs: `security_hardening_test.mjs` 25/25 + bestaande isolatietests groen (toolbox 27, bibliotheek 34, inspectie 25). Daarvóór dezelfde dag: **Juridische export voor de toolbox-module** (migraties 0020-0021). Twee exports, beide UITSLUITEND uit het onveranderlijke snapshot (volledig reconstrueerbaar ook nadat de centrale toolbox is gearchiveerd): een individueel **bewijsstuk** (PDF per deelname, met de handtekening zichtbaar) en een **bedrijfsoverzicht** (CSV + PDF-lijst over een periode). Eerlijke bewijssoort-vermelding ('digitaal' = eigen handtekening / 'fysiek_aanwezig' = aanwezigheidsregistratie), neutrale formulering (geen "gekwalificeerde handtekening"). Export-RPC's zijn lees-only, SECURITY DEFINER, alleen voor authenticated KAM/admin van het eigen bedrijf — niet voor anon/werknemer-token. **Onderweg een echte beveiligingslek gevonden en gedicht** (zie open punt): de isolatietest betrapte dat een anon-caller de export kon bereiken doordat `mag_bedrijf_beheren` zonder auth NULL teruggeeft en `if not null` de raise oversloeg; opgelost met `coalesce(...,false)` + EXECUTE intrekken van anon. Toolbox-export-isolatietest 27/27 groen. Daarvóór (27 juni): **Toolbox-module met aantoonbare deelname + live naar-rato-dashboard** (migraties 0015-0019). Een nieuwe afneembare module (catalogus 'toolbox'): centrale toolboxen (tekst + video-link + optionele quiz, admin beheert, klant koppelt/wijkt lokaal af zoals de bibliotheek), een werknemer volgt via zijn persoonlijke link (lezen → video → quiz → naam bevestigen → vinger-handtekening → afronden) wat een ONVERANDERLIJK bewijsrecord oplevert (snapshot + tijd + naam + handtekening; bewijssoort 'digitaal' nu, 'fysiek_aanwezig' + presentielijst-upload gereserveerd), en een live dashboard dat per persoon/functiegroep/bedrijf de voortgang toont tegen een naar-rato-doel (instroom → lager doel, uitstroom → uit de noemer). Isolatie-/beveiligings-/naar-rato-test 19/19 groen. AVG-punten toegevoegd (zie onderaan). Daarvóór dezelfde dag: **centrale inspectie-bibliotheek met lokaal afwijken** gebouwd en getest (migraties 0007-0014).
>
> _Oudere stand:_ centrale inspectie-bibliotheek (migraties 0007-0012). Eén centrale norm (rubrieken + vragen, beheerd door admin, gedeeld over alle klanten); een klant koppelt rubrieken en mag op eigen initiatief — met waarschuwing — lokaal afwijken (eigen tekst of uitzetten). Een centrale wijziging werkt automatisch door waar niet is afgeweken, en geeft een zichtbaar signaal waar wél is afgeweken (met "overnemen" of "mijn versie houden"). Inspectie starten kan nu vanuit de norm (snapshot van geldende vraag + rubrieknaam) náást het bestaande vrije sjabloon. Beveiliging: centrale tabellen leesbaar voor iedereen-ingelogd, schrijfbaar alleen door admin; per-bedrijf-koppeling/afwijking alleen-lezen + uitsluitend muteerbaar via de RPC's; isolatietest 28/28 groen, inspectie-regressietest 25/25. Daarvóór dezelfde sessie: functiegroepen op personen + doel-functiegroep op sjabloon (migratie 0006, isolatie 25/25), en logo-kleur "uit logo halen" als bevestigd voorstel op de huisstijlpagina. Eerder (26 juni): rapporten-bibliotheek + rijke rapportweergave gebouwd en getest (twee alleen-lezen RPC's, archiefoverzicht per bedrijf + nette rapportpagina; isolatietest 13/13 groen tegen de live DB). TLS-valkuil voor de REST/HTTPS-testscripts nu netjes opgelost: draaien met `node --use-system-ca …` (gebruikt de Windows-certstore) i.p.v. de onveilige globale `NODE_TLS_REJECT_UNAUTHORIZED=0` — die bypass is uit de scripts verwijderd. Opruimklus: `db/inspectie_stap2_rapporten_rpcs.sql` → `db/inspectie_rapporten_rpcs.sql` hernoemd (de "stap2" botste met de échte inspectie-stap 2). Module-zelfbeheer-terminologie blijft bewust neutraal (geen 'abonnement'); module-isolatietest opnieuw groen (8/8). Eerder: module-zelfbeheer per bedrijf (drie toestanden, eerste keer = bewuste activatie, stopzetten apart; nog géén facturatie) gebouwd; KAM-dashboard browsergetest; werkplekinspectie-module live (alpha); managementdashboard gebouwd (KAM + admin); directe DB-tooling + schemadump als bron van waarheid. Open: rapporten-bibliotheek + modulebeheer nog door de browser halen; Geissler-termijnanker; rie_versie-backfill; admin-dashboard browsertest.

## Het project in het kort

Twee sporen onder één dak:
- **Spoor 1 — De RI&E (de Toverspreuk):** vakmodel, datamodel, generator, intake. Kernlijn: AI bereidt voor, Kees toetst.
- **Spoor 2 — Het klantportaal:** Next.js/Supabase-app waar de getoetste RI&E en het Plan van Aanpak gaan leven.

De naad tussen beide is het belangrijkste open punt: delen ze één datamodel? Zie "Overkoepelend".

## Spoor 1 — RI&E: waar staan we

- Vakmodel/Toverspreuk: ingevuld en getest. P3 hernoemd naar "Ingrijpende gebeurtenissen en PSA-incidenten" (Beslissing 51), O3-10 thuiswerkersvraag toegevoegd (Beslissing 52).
- Datamodel: schema v1.1 vastgesteld als JSON Schema (Beslissing 36). `geissler_all.json` is de canonieke referentie-dataset.
- Consistentie-checker (`check_dataset.py`): klaar en getest op Geissler (Beslissing 37). Controleert structuur, kruisverwijzingen, Kinney-berekening (R = E×B×W), dodelijk-regel en lege samenvattingsvelden.
- Vragenbibliotheek: 140 vragen, 12 modules, generiek en bedrijfsneutraal (Beslissing 40).
- Intake-wizard: gebouwd en getest op een nep-bedrijf (correcte module-activering, aanpakniveau, kwetsbare groepen). Na test verbeterd: nooit vragen bundelen ook bij ongeduld, actieve inferentie uit eerdere antwoorden, adres in één vraag, geen AI-frases.
- Generator (`build_qvox.py`): functioneel, levert het juiste format, maar leest de nieuwe velden (kinney, brf_categorie, toetsbrief) nog niet. Uitbreiden.
- Geissler-rapport: 57 pagina's, FROZEN v6, definitief en opgeleverd.
- Docx-import-pipeline: eerste echte klant-RI&E ingeladen via de keten docx → `dataset.json` (schema v1.1) → `check_dataset.py` (0 fouten) → `import_company(jsonb)`, met aparte `rie_versies`-rij (versie 1, vrijgegeven). Bevindingen/knelpunten/PvA letterlijk uit de docx; geen Kinney-scores verzonnen waar de docx die niet gaf. Herbruikbare scripts in `import/` (extract_docx, build_dataset, import_run); klant-specifieke input (bron-docx, dataset.json, bedrijf.json) blijft lokaal via `.gitignore`.

## Spoor 2 — Klantportaal: waar staan we

Werkende Next.js/Supabase-app, in twee browsertest-ronden groen op de kritieke punten. Wat staat:
- Rollen: admin (Kees), KAM-coördinator (ziet eigen bedrijf), actiehouder (geen account, geheime link).
- Vrijgavemodel: actiehouder dient voorstel in, KAM geeft vrij of stuurt terug; de officiële status is alleen via dat proces wijzigbaar (Beslissing 41-43).
- Volledige audit-trail via een loggende RPC-laag (Beslissing 44); geen datalek tussen bedrijven (getest).
- White-label per klant (logo/kleur/lettertype); veilige standaard: uit tenzij geactiveerd (Beslissing 46-50).
- Bewijs-upload (foto/PDF, beveiligd, naspeurbaar, auto-verkleind), acties doorgeven aan een collega, mobiel.
- E-mailmeldingen (toewijzen/doorgeven) + herinneringen (handmatig + automatisch, met maximum per persoon en logboek): gebouwd.
- **Werkplekinspectie-module (alpha):** sjabloonbeheer + inspectie uitvoeren via een loggende RPC-laag; niet-in-orde-bevindingen worden PvA-acties. Per bedrijf aan/uit (`bedrijf_modules`). Database-E2E 18/18, negatieve isolatietests 9/9 en de nachttest groen tegen de live DB; de schermen laden en de dashboard-tegels klikken door. Nog niet met echte data door de browser gehaald.
- **Managementdashboard:** KAM per bedrijf (`/[company_id]/dashboard`, white-label) + admin-roll-up (`/dashboard`), gevoed door één lees-RPC `dashboard_overzicht` / `dashboard_admin_overzicht`. Tegels: te beoordelen, voortgang PvA, termijn-urgentie, prioriteit, RI&E-geldigheid, inspecties, bewijslast. **KAM-dashboard browsergetest (20 juni): alle 7 tegels klikken door naar de juiste pagina; de RI&E-geldigheid-tegel is gerepareerd (linkt nu naar `/rie`).**
- **Rapporten-bibliotheek + rijke rapportweergave (26 juni):** een archiefoverzicht per bedrijf van alle (uitgevoerde en lopende) werkplekinspecties, en een nette rapportpagina per inspectie. Gevoed door twee alleen-lezen RPC's (`inspectie_bibliotheek` / `inspectie_rapport`, `db/inspectie_rapporten_rpcs.sql`, SECURITY DEFINER, autorisatie via `mag_bedrijf_beheren`; het bedrijf wordt server-side uit de inspectie afgeleid, nooit van de client vertrouwd). UI: bibliotheek op `/[company_id]/inspecties` en rapport op `/[company_id]/inspecties/[inspectie_id]` (`InspectieRapport`). Geen schemawijziging — puur lees-RPC's. Isolatietest 13/13 groen (`scripts/inspectie_isolatie_test.mjs`, incl. 4 nieuwe rapporten-checks). Nog niet door de browser gehaald.
- **DB-tooling:** directe SQL-runner (`scripts/db_run.mjs`) en schemadumper (`npm run db:schema` → `db/schema.sql`) als reviewbare bron van waarheid naast `supabase/migrations`. De `pg`-tooling (db_run/db:schema) gaat via TCP en heeft geen TLS-truc nodig; de REST/HTTPS-testscripts (isolatie-/nachttests) draai je met `node --use-system-ca …` zodat Node de Windows-certstore gebruikt (lost de corporate TLS-onderscheppingsproxy op zonder de onveilige `NODE_TLS_REJECT_UNAUTHORIZED=0`-bypass).
- **Laad-optimalisatie (20 juni):** elke beveiligde pagina heeft nu een `loading.tsx`-skelet (gedeelde `components/LaadSkeleton`), zodat een klik direct reageert i.p.v. 'dood' aanvoelen; onafhankelijke Supabase-leesacties per pagina draaien parallel via één `Promise.all` i.p.v. een waterval; en `koppel_mij_als_persoon` draait niet meer bij élke PvA-/Personen-lading maar alleen als de KAM nog ontbreekt (`lib/personen-data.ts`). Autorisatie ongewijzigd; tsc + build groen.
- **Module-zelfbeheer per bedrijf (20 juni):** per bedrijf hebben modules nu drie toestanden in `bedrijf_modules.module_status` — niet actief (`geen`) / actief / gestopt — bovenop de bestaande gebruiks-toggle `actief` (aan/uit = gebruik pauzeren). De eerste activatie is een bewuste stap (bevestiging, `geactiveerd_op` vastgelegd); stopzetten is een eigen knop (`gestopt_op`). Nog géén facturatie — alleen status + momenten, zodat dat er later op kan leunen. **Bewuste keuze: geen 'abonnement'-terminologie**, ook niet in de DB/RPC's (zie migratie 0005). Beheerscherm `/[company_id]/modules` (`ModuleBeheer`, echte buttons/links) voor wie het bedrijf mag beheren; RPC-laag `module_activeren` / `module_gebruik_zetten` / `module_stopzetten` (`db/module_zelfbeheer_rpcs.sql`, SECURITY DEFINER, `mag_bedrijf_beheren`-check, auditlog in `module_historie`). Nav + dashboard tonen een module alleen bij `module_status='actief'` én `actief=true`. Isolatietest `scripts/module_isolatie_test.mjs` 8/8 groen. Nog niet door de browser gehaald.

- **Centrale inspectie-bibliotheek + lokaal afwijken (27 juni):** dé norm voor de werkplekinspectie staat nu centraal en gedeeld. Datamodel (migratie 0007): `centrale_rubriek` + `centrale_vraag` (geen `company_id`, `versie`/`gewijzigd_op` voor doorwerking-signalering), `bedrijf_rubriek` (koppeling op rubriekniveau) en `bedrijf_vraag_afwijking` (lokaal/uit + `basis_versie`, alleen de echte afwijkingen). `inspectie_bevinding` kreeg `rubriek_naam_snap` voor de snapshot. **Beveiliging:** centrale tabellen leesbaar voor elke ingelogde gebruiker, schrijfbaar alleen door `role=admin` (merken-patroon); per-bedrijf-tabellen alleen-lezen via RLS en **uitsluitend muteerbaar via de SECURITY DEFINER-RPC's** (0012), met `mag_bedrijf_beheren` + cross-company-guard — een klant kan niet stiekem of buiten zijn eigen bedrijf afwijken. **Beheer (admin):** `/admin/bibliotheek` (rubrieken/vragen toevoegen, hernoemen, herordenen, archiveren, intern `rie_code`-veld dat de uitvoerder nooit ziet), RPC's `centrale_rubriek_opslaan/-archiveren` + `centrale_vraag_opslaan/-archiveren` (versie hoogt alleen bij een echte inhoudswijziging). **Klant:** nieuwe **Norm**-tab bij Werkplekinspectie — rubrieken koppelen, en per vraag op een expliciete knop (mét waarschuwing vooraf) lokaal aanpassen of uitzetten; afwijking is zichtbaar gemarkeerd; bij een gewijzigde norm een signaal met "overnemen (terug naar centraal)" of "mijn versie houden". RPC's `rubriek_koppelen/-ontkoppelen`, `vraag_lokaal_aanpassen`, `vraag_uitzetten`, `vraag_terug_naar_centraal`, `bedrijf_norm_overzicht`. **Uitvoeren:** `inspectie_start_centraal` bevriest de geldende vraagtekst + rubrieknaam per bevinding; uitvoer- en rapportweergave groeperen per rubriek; `rie_code` blijft verborgen. De vrije eigen sjablonen blijven bestaan als alternatief pad — de startkaart toont beide duidelijk gescheiden ("volgens de norm" vs "eigen sjabloon"). Startset: de 7 centrale rubrieken (21 vragen) geseed (migratie 0011). Isolatie-/beveiligingstest `scripts/centrale_bibliotheek_isolatie_test.mjs` 28/28 groen; inspectie-regressietest 25/25. **Nog niet door de browser gehaald.**

- **Toolbox-module (27 juni):** aantoonbare toolbox-deelname + live doelstellingen-dashboard, als nieuwe afneembare module (catalogus 'toolbox', `/[company_id]/toolbox`). **Datamodel (0015):** centraal `centrale_toolbox` (tekst, video-LINK, telt-mee-instellingen: vereist_video/quiz, slaaggrens, uitleg-modus, toegang link/login) + `centrale_toolbox_vraag` (quiz met opties/juist/uitleg); per bedrijf `bedrijf_toolbox` (koppeling), `bedrijf_toolbox_afwijking` (lokaal/uit), `bedrijf_doelstelling` (N per functiegroep per jaar). Dienstdata op personen (`datum_in_dienst/uit`, nullable). **Bewijsrecord `toolbox_deelname`:** snapshot van geldende tekst/video/quiz + tijd + naam-bevestiging + handtekening (base64 in DB), `bewijssoort` ('digitaal' nu / 'fysiek_aanwezig' gereserveerd), `presentielijst_pad` (Storage, gereserveerd), `planning_toewijzing_id` (haak planningsmodule). **Onveranderlijk:** een BEFORE UPDATE-trigger weigert elke wijziging — voor iedereen, ook admin/service_role (bewezen in de test). **Beveiliging:** centraal admin-only schrijfbaar/iedereen leest; per-bedrijf alleen-lezen + muteren via SECURITY DEFINER-RPC's met cross-company-guard; deelname aanmaken uitsluitend via de token-RPC (`toolbox_afronden_token`, persoon afgeleid uit het token, server kijkt de quiz na, dwingt telt-mee af, bevriest de naam). **Naar-rato (0018):** doel = round(N × dienstdagen-in-jaar / jaardagen); instroom 1 juli bij N=12 → 6; uitstroom stopt het doel en haalt de persoon uit de bedrijfs-noemer (blijft zichtbaar met status). **Beheer:** admin `/admin/toolboxen` (toolboxen + quiz + instellingen); KAM-tabs Dashboard/Toolboxen/Doelstellingen + in/uit-dienst bij Personen. **Werknemer:** `/tb/[token]` met de volledige flow + naam-mismatch-stop. Startset (0019): 3 voorbeeld-toolboxen met placeholder-videolinks + quizvragen, en voor Alpha de module aangezet, toolboxen gekoppeld en voorbeeld-doelen gezet. Test `scripts/toolbox_isolatie_test.mjs` 19/19. **Nog niet door de browser gehaald.** **Openstaand binnen de module:** (1) login-variant van de werknemer-flow voor toolboxen met `toegang='login'` (datamodel + filter staan klaar; de aparte ingelogde-route is nog niet gebouwd — link-toolboxen werken volledig); (2) de hele 'fysiek_aanwezig'-flow (zaal-registratie door een beheerder + presentielijst-upload) — alleen gereserveerd; (3) juridische export van het bewijs (met eerlijke bewijssoort-vermelding) nog te bouwen; (4) placeholder-videolinks vervangen door echte video's; (5) browsertest van de hele keten.

- **Toolbox — juridische export (28 juni):** bewijsmateriaal voor ongevalsonderzoek, **uitsluitend uit het onveranderlijke snapshot** in `toolbox_deelname` (RPC's joinen nooit naar `centrale_toolbox` — alleen `companies` voor de bedrijfsnaam), dus volledig reconstrueerbaar ook nadat de centrale toolbox is gewijzigd/gearchiveerd (getest). **Twee exports:** (1) individueel **bewijsstuk** — print/PDF-pagina `/[company_id]/toolbox/bewijs/[deelname_id]` met kerngegevens, bevroren tekst + video-link, quizvragen met gegeven/juiste antwoorden + uitslag, en de **handtekening-afbeelding zichtbaar**; (2) **bedrijfsoverzicht** — tab "Bewijs & export" met **CSV**-download (UTF-8 BOM, `;`-gescheiden) + **PDF-lijst** (`/[company_id]/toolbox/overzicht?van&tot`) over een kiesbare periode, met per regel een "Bewijs (PDF)"-link. **Eerlijke bewijssoort-zin** op beide ('digitaal' / 'fysiek_aanwezig'), neutrale verantwoording ("aantoonbare digitale bevestiging", geen "gekwalificeerde handtekening"). **Toegang:** lees-only `SECURITY DEFINER`-RPC's `toolbox_bewijs` / `toolbox_bewijs_overzicht`, `mag_bedrijf_beheren`-check, EXECUTE alleen voor `authenticated` + `service_role` (geen anon → geen export via werknemer-token); cross-company geweigerd. Aanpak: client-side `window.print()` (zelfde patroon als het inspectierapport), geen server-PDF-library. Isolatietest `scripts/toolbox_isolatie_test.mjs` 27/27. **Nog niet door de browser gehaald.**

## Wat ligt open

### Spoor 1 (RI&E)
- Generator uitbreiden voor de nieuwe velden (kinney, brf_categorie, toetsbrief, scope-matrix met HVK-groen, gekleurde klasse-cellen, PvA-kolom, bijlagen in de inhoudsopgave).
- ~~O2-3 en P2-4 in Geissler: brf + klasse goedkeuren (Beslissing 38, openstaand).~~ — vastgezet bij de oplevering (FROZEN v6); de definitieve brf/klasse staan in de Geissler-dataset.
- ~~Geissler actie 9 hangt aan een Ja-vraag (F3-17): borgingsactie, of moet F3-17 naar Nee?~~ — beslecht bij de oplevering (FROZEN v6).
- ~~Geissler foto-captions: eerdere feedback mogelijk onvolledig verwerkt, nalopen vóór oplevering.~~ — verwerkt bij de oplevering (FROZEN v6).
- ~~Geissler oplevering: certificaatnummer, datum, PvA-verantwoordelijken + deadlines, ondertekening toetsbrief.~~ — opgeleverd (FROZEN v6).
- O3-10 nog doorvoeren in de vragenbibliotheek (voorgestelde E-waarde 3).
- Laag 2 (AI-assistentie per module): uitgesteld tot na alignment.
- Wettelijke meetlat compleet maken (artikel-voor-artikel) + review-routine vastleggen (Beslissing 14).
- Wettelijke meetlat: thuiswerk citeert nu Arbobesluit 2.34/2.34a, terwijl Beslissing 52 1.43-1.53/1.47/1.48 noemt. Welke artikelen kloppen is jouw vakoordeel.
- Vakinhoudelijke meetlat: eigen dekking spiegelen aan gevestigde instrumenten (eenmalig).

### Spoor 2 (portaal)
- **Geissler-termijnanker (vakoordeel, wacht op Kees):** 18 open PvA-acties bij Geissler hebben kwalitatieve termijnen ("binnen 12 maanden"/"binnen 2 jaar") i.p.v. data; Geissler heeft géén rie_versie met toetsdatum. Kees moet een ankerdatum + horizon-conventie geven, dan kan `termijn_datum` gevuld worden en gaat de "over de termijn"-tegel ook voor Geissler leven. De RPC/UI staat klaar.
- **Backfill rie_versie_id (nog te schrijven):** bestaande modules/vragen/pva_items/fotos nog niet aan een rie_versie_id gekoppeld. Beslissing: wel backfillen. Migratie nog te schrijven, eerst op Alpha/Bravo. (De ooit voor deze backfill gereserveerde plek 0006 is inmiddels functiegroepen geworden en de migraties zijn doorgelopen t/m 0025 — de backfill wordt dus een volgende migratie.)
- ~~**Module-zelfbeheer (migraties 0004 + 0005)**~~ — gebouwd + gecommit (zie "waar staan we"). 0004 zette het drie-toestanden-model neer; 0005 hernoemde de terminologie naar neutrale termen (geen 'abonnement': kolom `module_status`, waarde `gestopt`, RPC's `module_activeren` / `module_stopzetten`). Beide + `db/module_zelfbeheer_rpcs.sql` gedraaid tegen de live DB; de Alpha-rij (inspectie, actief=true) is geïnterpreteerd als actieve module (`module_status='actief'`, `geactiveerd_op` gestempeld op migratiemoment want er was geen historisch startmoment, `actief` bleef true). Isolatietest 8/8, testdata opgeruimd. **Resteert:** browsertest van het beheerscherm (activeren → aan/uit → stopzetten → opnieuw activeren) met een echte KAM-login.
- ~~**Rapporten-bibliotheek (eerstvolgende bouwstap)**~~ — gebouwd (26 juni, zie "waar staan we"): archiefoverzicht per bedrijf + rijke rapportpagina per inspectie, twee alleen-lezen RPC's, isolatie 13/13. **Resteert:** door de browser halen (bibliotheek openen, een rapport bekijken) met een echte KAM-login. De bredere ambitie — óók de getoetste RI&E-rapporten en losse documenten per bedrijf op één plek — bouwt hierop voort.
- ~~**Functiegroepen (bouwstap)**~~ — gebouwd (27 juni, migratie 0006): functiegroep per persoon + doel-functiegroep op een sjabloon, als bouwsteen; isolatie 25/25. De koppeling "wijs deze inspectie toe aan wie deze rol heeft" komt later.
- **Bijlagen bij vragen/acties (open):** foto/document bij inspectievragen/-bevindingen en PvA-acties. Nog te bouwen.
- **Centrale bibliotheek — browsertest + vervolg (open):** het hele centraal/lokaal-model nog met een echte admin- én KAM-login door de browser halen (admin: rubriek/vraag beheren; klant: koppelen, lokaal afwijken mét waarschuwing, een normwijziging zien doorwerken, inspectie starten vanuit de norm). Gedrag bij archiveren (migratie 0013): wie LOKAAL afweek houdt zijn eigen versie ook als de centrale vraag/rubriek wordt gearchiveerd — de vraag blijft in zijn norm-overzicht én in nieuwe inspecties, gemarkeerd als "centraal vervallen — je houdt je eigen versie" (knop "mijn versie verwijderen" volgt alsnog de norm). Wie de norm vólgde, ziet de gearchiveerde vraag verdwijnen; wie hem had uitgezet, houdt hem weg. Notificatie bij een centrale wijziging (migratie 0014, keuze Kees = in-app telbadge, geen e-mail): een telbadge op de **Norm**-tab en een regel op het bedrijfsdashboard tonen het aantal afwijkende punten waar de centrale norm is bijgewerkt (onbeantwoord). De teller telt alleen nog-actieve centrale vragen met `versie > basis_versie`; "overnemen" of "mijn versie houden" wist het signaal. De staande "centraal vervallen"-staat telt bewust niet mee (zou permanent branden). Vervolg (later, planning stap 2): inspectie automatisch toewijzen aan wie een bepaalde functiegroep heeft.
- **Inspectie stap 2 (ligt bij externe review):** planning, toewijzing en herinneringen rond een inspectie. Wacht op de uitkomst van de review voordat we bouwen. (Let op: de rapporten-/bibliotheeklaag heette ooit "stap 2" in de bestandsnaam; dat is hernoemd naar `db/inspectie_rapporten_rpcs.sql` om verwarring met déze stap 2 te voorkomen.)
- **Roadmap als losse modules:** audit-module en toolbox(-meeting) komen later als eigen activeerbare modules in de modulecatalogus (`lib/modules-catalogus.ts`).
- Werkplekinspectie verder uitbouwen (uit alpha): bredere uitrol, eventueel gast-/mobiele inspecties.
- Uitgebreid rollenmodel deskundigen (HVK/A&O/arbeidshygiënist): grote aparte uitbreiding, later.
- Gast de terugstuurreden tonen (kleine afronding).
- Leeg paneel rechtsboven (niet-kritiek, vermoedelijk externe widget).
- Middleware-deprecatie opruimen: Next 16 wil `proxy` i.p.v. `middleware` (dev-warning). Geen haast, maar de middleware regelt auth-redirects — voorzichtig migreren.
- Admin-roll-up dashboard (`/dashboard`) nog niet in de browser nagelopen (alleen KAM-dashboard per bedrijf is browsergetest). Inloggen als admin, elke bedrijfsregel aanklikken → `/[bedrijf]/dashboard`.
- Nevengevolg van de nieuwe `loading.tsx`-skeletten: door streaming kan een `notFound()` ná de await geen echte 404-status meer zetten; de gebruiker ziet wél de niet-gevonden-pagina (met `noindex`). Voor dit interne portaal prima; alleen relevant als er ooit een harde 404-status nodig is.
- **Schema in repo:** supabase/migrations/ (0001-0025) + `db/schema.sql` als dump. Grofweg: 0001-0003 rie_versies / pva_termijn / revoke-execute, 0004-0005 module-zelfbeheer, 0006 functiegroepen, 0007-0014 centrale bibliotheek, 0015-0021 toolbox + juridische export, 0022-0023 security-hardening (Beslissing 62), 0024 toolbox eens-per-jaar, 0025 incident-datamodel. Toekomstige schema-wijzigingen altijd als migratiebestand committen.
- **Parallelle nachttest-agent:** een tweede agent op main draait tenant-isolatietests (`scripts/nachttest_rls.mjs`). Vond echte lekken en dichtte ze in migratie `0003_revoke_execute_interne_rpcs.sql`: REVOKE EXECUTE op 4 interne helpers (actie_als_jsonb lekte volledige pva-rijen; import_rie_content was destructief; import_company; vind_of_maak_persoon) — alleen service_role houdt toegang. Geverifieerd dat dit géén client-facing RPC's raakt (dashboard/PvA/inspectie blijven werken). Legitieme beveiliging, geen inperking van functionaliteit.
- ~~Kapot logo~~ — opgelost: /logo.jpg geeft 200 (geldig JPEG aanwezig in /public, geverifieerd 19 juni).
- ~~E-mailmeldingen + herinneringen~~ — gebouwd (zie boven).
- ~~Managementdashboard~~ — gebouwd + KAM-dashboard browsergetest (20 juni, alle tegels klikbaar). Resteert: admin-dashboard browsertest, test op fysieke devices, en de smaaksuggesties (iconen, 3-koloms op breed scherm).

### Overkoepelend / blokkerend
- **AVG (blokkeert livegang):** verwerkersovereenkomst, verwerkingsregister, privacyverklaring (Beslissing 33). Datalocatie goed: Supabase EU West (Ierland).
- **AVG — nieuw met de toolbox-module (toevoegen aan het register/de verklaring):**
  - **Handtekening-afbeelding** (`toolbox_deelname.handtekening`): gevoelig, identificerend persoonsgegeven (bewijs van instructie). Toegang beperkt tot het eigen bedrijf (`mag_bedrijf_beheren`) + de persoon zelf via token; nooit cross-company. Het record is onveranderlijk; verwijderrecht = rij verwijderen (cascade bij persoon/bedrijf). Bewaartermijn vastleggen.
  - **Bewijs-PDF / export** (toolbox-export, 28 juni): de export bundelt **naam + handtekening + instructie-inhoud** in een downloadbaar/afdrukbaar document. Een bewijs-PDF is daarmee gevoelig persoonsgegeven dat het systeem kan verlaten (download/print/mail). Vastleggen: bewaartermijn, wie mag exporteren (nu: KAM/admin eigen bedrijf), en bewust omgaan met verspreiding buiten het portaal. De export zelf is lees-only en muteert niets.
  - **In/uit-dienstdatum** (`personen.datum_in_dienst/uit`): persoonsgegeven over het dienstverband; alleen zichtbaar voor het eigen bedrijf.
  - **Presentielijst-upload** (`presentielijst_pad`, nog te bouwen): AVG-ZWAAR — één document met handtekeningen van MEERDERE personen. Strengere toegang dan een eigen handtekening, expliciete bewaartermijn, en een afscherm-/zwartmaak-routine want het verwijderrecht van één persoon raakt een document waar ook anderen op staan. Vastleggen vóórdat de fysieke variant live gaat.
- **AVG — nieuw met de incidenten-module (ZWAARSTE categorie tot nu toe; toevoegen aan het register/de verklaring):**
  - **Gezondheidsgegevens** (`incident.functie_slachtoffer`, `incident.medische_dienst_bezocht`, en het gevolg `letsel`): bijzonder persoonsgegeven (art. 9 AVG). Staan uitsluitend op de `incident`-rij, die alleen leesbaar is voor de KAM/coördinator van het **eigen** bedrijf (`mag_bedrijf_beheren`) — nooit cross-company. Ze komen **bewust NIET** in de open Deel-1-meldflow: de melder (anon, geen login) legt ze niet vast en kan de rij niet teruglezen. Bewaartermijn te bepalen.
  - **Incident-foto's** (`incident_foto`): kunnen een herkenbaar persoon of letsel tonen → potentieel bijzonder persoonsgegeven. Opgeslagen in een **privé** Supabase Storage-bucket, **geen publieke URL**; toegang alleen via kortlevende service-role signed URLs ná `mag_bedrijf_beheren` (KAM) of ná token-validatie (melder bij uploaden). Padconventie bedrijf-geprefixt zodat de bestaande per-bedrijf `storage.objects`-RLS ze ook bij directe API-toegang afschermt. Bewaartermijn + verwijderroutine te bepalen.
  - **Naam melder** (`incident.naam_melder`, optioneel): laaggehouden drempel — mag leeg. Persoonsgegeven; alleen zichtbaar voor het eigen bedrijf.
  - **Meldlink-token** (`incident_meldlink.token`): een bedrijfseigen, intrekbaar/roteerbaar token — géén hergebruik van het werknemer-/actiehouder-token. Bij misbruik van een gedeelde meldlink kan de KAM het token vervangen zonder andere tokens te raken.
- ~~**⚠️ Beveiligingsbevinding — null-veilige RPC-guard**~~ — **opgelost (28 juni, migraties 0022/0023; Beslissing 62).** Het lek: `mag_bedrijf_beheren(...)` gaf voor een caller zónder auth `NULL` terug en `if not mag_bedrijf_beheren(...) then raise` ving die NULL niet af (`not null` = null → raise gemist), terwijl per-bedrijf-RPC's standaard óók EXECUTE voor `anon` hadden. **Driedubbel gedicht:** (1) **bron-fix** — `mag_bedrijf_beheren` doet nu `coalesce(..., false)`, dus elke huidige én toekomstige guard is veilig; (2) **EXECUTE ingetrokken** voor anon/PUBLIC op alle 57 per-bedrijf/admin-RPC's (alleen `authenticated` + `service_role`); de gast/werknemer-**token-RPC's** en RLS-helpers bewust met rust gelaten; (3) **standaard voor nieuwe RPC's** vastgelegd in Beslissingen.md. Bewijs: `scripts/security_hardening_test.mjs` 25/25 (exhaustief + runtime + regressie + token-flow mét/zónder token); bestaande isolatietests blijven groen.
- **Opruimen vóór livegang:** de demo-/testaccounts en testbedrijven (Alpha/Bravo, demo-logins) weghalen zodat alleen echte klantdata overblijft.
- **Fotoprotocol:** welke foto's verplicht per aanpakniveau, datering en labeling (Beslissing 31).
- Bewuste portaal-risico's bewaken: doorgeven naar extern adres zonder seintje aan KAM; automatische mail; opslaggroei; geheime sleutels nooit in zichtbare code.
- Eigen QHSE Totaal-huisstijl uitwerken (Beslissing 54).
- ~~Alignment twee sporen~~ — opgelost 17 juni (Beslissingen 57-59).

## Werkwijze

> Kandidaat om naar de projectinstructie te verplaatsen (de aangescherpte onboarding). Dit zijn semi-permanente afspraken, geen momentopname.

- Kleine, testbare stappen; per grote stap eerst het SQL-bestand, dan de Claude Code-prompt.
- Diagnose vóór fix: een gemelde bug is een hypothese tot de oorzaak met bewijs is aangetoond.
- Verbindingscheck vóór testronden (één schrijf-actie; faalt die, eerst de Supabase-config op Vercel checken).
- Testen op twee niveaus: code-checks door de codeerhulp, visueel en gedrag door browsertest.
- Veilige standaard: nieuwe functies breken bestaande werking nooit; opt-in.
- Nieuwe per-bedrijf/admin-RPC's krijgen standaard géén anon/PUBLIC-EXECUTE — alleen `authenticated` + `service_role`, guards altijd null-veilig (`coalesce(..., false)`); token-flows zijn de bewuste uitzondering (Beslissing 62).

---

*Dit document vervangt de losse sessieoverzichten, overdrachten, chatsamenvattingen en projectexports. Maak voortaan geen nieuwe momentopname-bestanden meer: verwerk de oogst van een chat hier, of als besluit in `Beslissingen.md`.*
